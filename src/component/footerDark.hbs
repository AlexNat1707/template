{{#with footer}}
<footer class="footer">
    <div class="footer_links">
        {{#each pages}}
        <a href="{{path}}" class="pages_link">{{name}}</a>
        {{/each}}
    </div>
    <div class="footer_copyright">{{copyright}}</div>
</footer>
{{/with}}